<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.triharf.controllers.JeuSoloController"
            prefWidth="900.0" prefHeight="600.0"
            styleClass="root-stack"
            stylesheets="@../css/main.css">

    <!-- Header -->
    <top>
        <HBox alignment="CENTER" prefHeight="80.0" spacing="30.0" styleClass="game-header">
            <padding>
                <Insets left="20" right="20"/>
            </padding>
            <children>
                <!-- Back Button -->
                <Button fx:id="btnRetour" text="←" styleClass="btn-back"/>

                <!-- Timer Box -->
                <HBox alignment="CENTER" spacing="10.0" styleClass="info-box">
                    <children>
                        <Label text="⏱" style="-fx-font-size: 20px; -fx-text-fill: white;"/>
                        <Label fx:id="lblTimer" text="00:00" styleClass="info-value">
                            <font><Font size="20.0"/></font>
                        </Label>
                    </children>
                </HBox>

                <!-- Letter Display -->
                <VBox alignment="CENTER" spacing="2">
                    <children>
                        <Label text="LETTRE" style="-fx-font-size: 10px; -fx-text-fill: #aaa; -fx-font-weight: bold;"/>
                        <Label fx:id="lblLettre" text="A" style="-fx-background-color: linear-gradient(to bottom, #9b59b6, #8e44ad); -fx-background-radius: 30; -fx-text-fill: white; -fx-padding: 8 20; -fx-font-weight: bold;">
                            <font><Font size="32.0"/></font>
                        </Label>
                    </children>
                </VBox>

                <!-- Title -->
                <Label text="MODE SOLO" styleClass="header-title"/>

                <!-- Score Box -->
                <HBox alignment="CENTER" spacing="10.0" styleClass="info-box">
                    <children>
                        <Label text="🏆" style="-fx-font-size: 20px; -fx-text-fill: white;"/>
                        <Label fx:id="lblScore" text="0" styleClass="info-value">
                            <font><Font size="20.0"/></font>
                        </Label>
                    </children>
                </HBox>
            </children>
        </HBox>
    </top>

    <!-- Main Content - Categories -->
    <center>
        <VBox alignment="TOP_CENTER" spacing="0" VBox.vgrow="ALWAYS">
            <children>
                <!-- Instruction Bar -->
                <HBox alignment="CENTER" style="-fx-background-color: rgba(155, 89, 182, 0.1); -fx-padding: 12;">
                    <children>
                        <Label text="📝 Entrez un mot commençant par la lettre pour chaque catégorie" style="-fx-text-fill: #ccc; -fx-font-size: 13px;"/>
                    </children>
                </HBox>

                <!-- Categories Container -->
                <ScrollPane fitToWidth="true" fitToHeight="true" VBox.vgrow="ALWAYS" styleClass="scroll-pane" style="-fx-background: transparent; -fx-background-color: transparent;">
                    <content>
                        <FlowPane fx:id="containerCategories" hgap="20.0" vgap="20.0" alignment="CENTER" style="-fx-padding: 30;" prefWrapLength="840.0">
                            <!-- Categories added dynamically -->
                        </FlowPane>
                    </content>
                </ScrollPane>
            </children>
        </VBox>
    </center>

    <!-- Bottom Action Bar -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 20; -fx-background-color: linear-gradient(to top, rgba(0,0,0,0.4), transparent);">
            <children>
                <Button fx:id="btnTerminer"
                        onAction="#handleTerminer"
                        text="✓ TERMINER LA PARTIE"
                        prefWidth="350.0" prefHeight="55.0"
                        styleClass="btn-action"
                        style="-fx-font-size: 18px;"/>
            </children>
        </HBox>
    </bottom>

</BorderPane>