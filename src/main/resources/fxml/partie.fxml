<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.triharf.controllers.JeuSoloController"
      alignment="TOP_CENTER"
      prefWidth="900.0" prefHeight="600.0"
      spacing="0"
      styleClass="root-vbox"
      stylesheets="@../css/main.css">

    <!-- En-tête violet -->
    <HBox alignment="CENTER" prefHeight="100.0" prefWidth="900.0" spacing="80.0" styleClass="game-header">
        <children>
            <!-- TIMER -->
            <HBox alignment="CENTER" spacing="10.0" styleClass="info-box">
                <children>
                    <Label text="⏱" style="-fx-font-size: 18px;" textFill="white"/>
                    <Label fx:id="lblTimer" text="" styleClass="info-value">
                        <font><Font size="18.0"/></font>
                    </Label>
                </children>
            </HBox>

            <!-- LETTRE -->
            <Label fx:id="lblLettre" text="" styleClass="letter-circle">
                <font><Font size="32.0"/></font>
            </Label>

            <!-- SCORE -->
            <HBox alignment="CENTER" spacing="10.0" styleClass="info-box">
                <children>
                    <Label text="🏆" style="-fx-font-size: 18px;" textFill="white"/>
                    <Label fx:id="lblScore" text="" styleClass="info-value">
                        <font><Font size="18.0"/></font>
                    </Label>
                </children>
            </HBox>
        </children>
    </HBox>

    <!-- Corps avec les catégories -->
    <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS" styleClass="scroll-pane">
        <content>
            <VBox fx:id="containerCategories" spacing="12.0" style="-fx-padding: 20;">
                <!-- Categories added dynamically -->
            </VBox>
        </content>
    </ScrollPane>

    <!-- Bouton Terminer -->
    <Button fx:id="btnTerminer"
            onAction="#handleTerminer"
            text="📋 TERMINER"
            prefWidth="460.0" prefHeight="50.0"
            styleClass="btn-terminate">
        <VBox.margin>
            <Insets top="10.0" bottom="10.0" />
        </VBox.margin>
    </Button>

</VBox>