<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.triharf.controllers.JeuMultiController"
            prefWidth="900.0" prefHeight="600.0"
            styleClass="root-stack"
            stylesheets="@../css/main.css">

    <!-- En-tête -->
    <top>
        <VBox spacing="0">
            <children>
                <HBox alignment="CENTER" prefHeight="70.0" styleClass="game-header" spacing="40.0">
                    <children>
                        <!-- Bouton retour -->
                        <Button fx:id="btnBack" text="←" onAction="#handleBack" styleClass="btn-back"/>

                        <!-- TIMER -->
                        <HBox alignment="CENTER" spacing="10.0" styleClass="info-box">
                            <children>
                                <Label text="Temps:" styleClass="info-label"/>
                                <Label fx:id="lblTimer" text="03:00" styleClass="info-value">
                                    <font><Font size="18.0"/></font>
                                </Label>
                            </children>
                        </HBox>

                        <!-- LETTRE À TROUVER -->
                        <VBox alignment="CENTER" spacing="2">
                            <children>
                                <Label text="Lettre" styleClass="info-label" style="-fx-font-size: 10px;" />
                                <Label fx:id="lblLettre" text="A" style="-fx-background-color: linear-gradient(to bottom, #9b59b6, #8e44ad); -fx-background-radius: 30; -fx-text-fill: white; -fx-padding: 5 15; -fx-font-weight: bold;">
                                    <font><Font size="28.0"/></font>
                                </Label>
                            </children>
                        </VBox>

                        <Label text="PARTIE MULTIJOUEUR" styleClass="header-title" />

                        <!-- JOUEURS -->
                        <HBox alignment="CENTER" spacing="10.0" styleClass="info-box">
                            <children>
                                <Label text="👥" style="-fx-text-fill: white;"/>
                                <Label fx:id="lblJoueurs" text="2" styleClass="info-value"/>
                            </children>
                        </HBox>
                    </children>
                </HBox>


            </children>
        </VBox>
    </top>

    <!-- Contenu principal -->
    <center>
        <HBox spacing="15.0" style="-fx-padding: 20;" HBox.hgrow="ALWAYS">
            <children>
                <!-- Zone réponses - Table avec colonnes catégories et lignes joueurs -->
                <VBox spacing="0.0" HBox.hgrow="ALWAYS" styleClass="content-panel" style="-fx-padding: 0;">
                    <children>
                        <HBox spacing="0.0" style="-fx-background-color: rgba(255,255,255,0.05); -fx-padding: 10;" alignment="CENTER_LEFT">
                            <children>
                                <Label text="📝 RÉPONSES DES JOUEURS" styleClass="info-label" style="-fx-font-size: 16px;"/>
                                <Region HBox.hgrow="ALWAYS"/>
                                <Label fx:id="lblValidationStatus" text="" style="-fx-text-fill: #f39c12; -fx-font-size: 12px;"/>
                            </children>
                        </HBox>

                        <Separator styleClass="stats-separator" />

                        <ScrollPane fitToWidth="true" fitToHeight="true" styleClass="scroll-pane" VBox.vgrow="ALWAYS">
                            <content>
                                <VBox fx:id="vboxPlayers" spacing="0" style="-fx-padding: 0;">
                                    <!-- Header row with category names -->
                                    <HBox fx:id="hboxCategoryHeaders" spacing="0" style="-fx-background-color: rgba(142, 68, 173, 0.3); -fx-padding: 10;"/>
                                    <!-- Player rows container -->
                                    <VBox fx:id="vboxPlayerRows" spacing="5" style="-fx-padding: 10;"/>
                                </VBox>
                            </content>
                        </ScrollPane>

                        <!-- Bouton Valider -->
                        <HBox alignment="CENTER" spacing="15" style="-fx-padding: 15; -fx-background-color: rgba(0,0,0,0.2);">
                            <children>
                                <Button fx:id="btnValider" text="✓ VALIDER MES RÉPONSES" onAction="#handleValider"
                                        styleClass="btn-action" style="-fx-font-size: 14px; -fx-padding: 10 30;"/>
                                <Button fx:id="btnVoirResultats" text="🏆 VOIR LES RÉSULTATS" onAction="#handleVoirResultats"
                                        styleClass="btn-action" style="-fx-font-size: 14px; -fx-padding: 10 30; -fx-background-color: #f39c12;"
                                        visible="false" managed="false"/>
                            </children>
                        </HBox>
                    </children>
                </VBox>

                <!-- Barre de chat -->
                <VBox spacing="0.0" minWidth="220.0" maxWidth="280.0" styleClass="content-panel" style="-fx-padding: 0;">
                    <children>
                        <HBox alignment="CENTER" style="-fx-background-color: rgba(255,255,255,0.05); -fx-padding: 12; -fx-background-radius: 12 12 0 0;">
                            <children>
                                <Label text="💬 CHAT" styleClass="info-label"/>
                            </children>
                        </HBox>

                        <ScrollPane fitToWidth="true" fitToHeight="true" VBox.vgrow="ALWAYS" styleClass="scroll-pane">
                            <content>
                                <VBox fx:id="vboxMessages" spacing="10.0" style="-fx-padding: 10;"/>
                            </content>
                        </ScrollPane>

                        <HBox spacing="5.0" style="-fx-padding: 10; -fx-background-color: rgba(0,0,0,0.2); -fx-background-radius: 0 0 12 12;" alignment="CENTER">
                            <TextField fx:id="tfMessage" promptText="Message..." HBox.hgrow="ALWAYS" onAction="#handleSendMessage" />
                            <Button fx:id="btnSend" text="➤" onAction="#handleSendMessage" styleClass="btn-secondary"/>
                        </HBox>
                    </children>
                </VBox>
            </children>
        </HBox>
    </center>

</BorderPane>