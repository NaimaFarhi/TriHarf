<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox alignment="CENTER" maxHeight="-Infinity" maxWidth="-Infinity" minHeight="-Infinity" minWidth="-Infinity"
      prefHeight="600.0" prefWidth="900.0" spacing="0.0"
      styleClass="root-vbox"
      stylesheets="@../css/main.css"
      xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.triharf.controllers.ResultatsMultiController">

    <children>
        <!-- SCROLLABLE CONTENT -->
        <ScrollPane fitToWidth="true" styleClass="scroll-pane" VBox.vgrow="ALWAYS" style="-fx-background: transparent; -fx-background-color: transparent;">
            <content>
                <VBox alignment="TOP_CENTER" spacing="0.0">
                    <children>
                        <!-- En-tête -->
                        <VBox alignment="CENTER" prefHeight="120.0" prefWidth="900.0" spacing="10.0" style="-fx-background-color: linear-gradient(to bottom, #f39c12, #e67e22);">
                            <children>
                                <Label text="🏆" style="-fx-font-size: 48px;" />
                                <Label text="CLASSEMENT FINAL" style="-fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 32px;" />
                                <Label fx:id="lblLettre" text="Lettre: A" style="-fx-text-fill: white; -fx-font-size: 16px;" />
                            </children>
                        </VBox>

                        <!-- Podium / Ranking -->
                        <VBox alignment="CENTER" spacing="20.0" style="-fx-padding: 30 40;">
                            <children>
                                <!-- Podium for top 3 -->
                                <HBox fx:id="hboxPodium" alignment="BOTTOM_CENTER" spacing="20" style="-fx-padding: 20;">
                                    <!-- Will be populated dynamically -->
                                </HBox>

                                <Separator style="-fx-opacity: 0.2;" />

                                <!-- Full ranking list -->
                                <Label text="Classement complet" styleClass="label-bright" style="-fx-font-size: 18px;" />
                                <VBox fx:id="vboxRanking" spacing="8" style="-fx-padding: 10;" />
                            </children>
                        </VBox>

                        <!-- Your result highlight -->
                        <VBox fx:id="vboxYourResult" alignment="CENTER" spacing="10" style="-fx-padding: 20; -fx-background-color: rgba(155, 89, 182, 0.2); -fx-border-color: #9b59b6; -fx-border-radius: 10; -fx-background-radius: 10; -fx-margin: 20;">
                            <children>
                                <Label text="Votre résultat" style="-fx-text-fill: #9b59b6; -fx-font-size: 14px;" />
                                <Label fx:id="lblYourRank" text="#1" style="-fx-text-fill: white; -fx-font-size: 36px; -fx-font-weight: bold;" />
                                <Label fx:id="lblYourScore" text="0 points" style="-fx-text-fill: #f39c12; -fx-font-size: 20px;" />
                            </children>
                        </VBox>
                    </children>
                </VBox>
            </content>
        </ScrollPane>

        <!-- BOTTOM BUTTONS (Fixed) -->
        <HBox alignment="CENTER" spacing="20.0" style="-fx-padding: 20; -fx-background-color: rgba(0,0,0,0.2);">
            <children>
                <Button fx:id="btnRejouer" onAction="#handleRejouer" styleClass="btn-action" text="🎮 REJOUER" />
                <Button fx:id="btnMenu" onAction="#handleMenu" styleClass="btn-secondary" text="MENU PRINCIPAL" />
            </children>
        </HBox>
    </children>
</VBox>
