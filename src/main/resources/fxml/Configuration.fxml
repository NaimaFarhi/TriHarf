<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox alignment="TOP_CENTER"
      prefHeight="400.0"
      prefWidth="600.0"
      spacing="5.0"
      style="-fx-background-color: #80808047; -fx-padding: 20;"
      xmlns="http://javafx.com/javafx/8"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.triharf.controllers.ParametresGenerauxController">

   <!-- DÉCLARATION DU TOGGLEGROUP -->
   <fx:define>
      <ToggleGroup fx:id="langueGroup"/>
   </fx:define>

   <children>

      <!-- HEADER -->
      <HBox alignment="CENTER_LEFT"
            prefHeight="20.0"
            prefWidth="560.0"
            style="-fx-background-color: #2c3e50; -fx-padding: 15;">

         <children>

            <Button text="←"
                    fx:id="btnRetour"
                    onAction="#handleRetour"
                    style="-fx-background-color: transparent;"
                    textFill="WHITE">
               <font>
                  <Font size="24.0"/>
               </font>
            </Button>

            <Label text="⚙ Paramètres" textFill="WHITE">
               <font>
                  <Font size="24.0"/>
               </font>
               <HBox.margin>
                  <Insets left="140.0"/>
               </HBox.margin>
            </Label>

         </children>
      </HBox>

      <!-- LANGUE -->
      <VBox prefHeight="69.0" prefWidth="560.0" style="-fx-background-color: white;">
         <children>

            <Label text=" Langue:"/>

            <HBox prefHeight="33.0" prefWidth="560.0" spacing="130.0">
               <children>

                  <RadioButton text="Français"
                               fx:id="rbFr"
                               toggleGroup="$langueGroup"
                               onAction="#handleLangue"/>

                  <RadioButton text=" العربية"
                               fx:id="rbAr"
                               toggleGroup="$langueGroup"
                               onAction="#handleLangue"/>

                  <RadioButton text="English"
                               fx:id="rbEn"
                               toggleGroup="$langueGroup"
                               onAction="#handleLangue"/>

               </children>
            </HBox>

         </children>
      </VBox>

      <!-- PSEUDO -->
      <VBox prefHeight="68.0" prefWidth="560.0" style="-fx-background-color: white;">
         <children>

            <HBox spacing="10.0">
               <children>
                  <Label text="👤"/>
                  <Label text="Pseudo:"/>
               </children>
            </HBox>

            <TextField fx:id="txtPseudo"
                       promptText="Entrez votre pseudo..."/>

         </children>
      </VBox>

      <!-- SON -->
      <VBox prefHeight="84.0" prefWidth="560.0" style="-fx-background-color: white;">
         <children>

            <HBox spacing="10.0">
               <children>
                  <Label text="🔊"/>
                  <Label text="Son:"/>
               </children>
            </HBox>

            <HBox spacing="15.0">

               <CheckBox text="Activer le son"
                         fx:id="chkSon"
                         selected="true"
                         onAction="#handleSon"/>

               <Slider min="0" max="100" value="70"
                       fx:id="sliderVolume"
                       onMouseReleased="#handleVolume"/>

               <Label text="70%"
                      fx:id="lblVolume"/>

            </HBox>

         </children>
      </VBox>

      <!-- JOUER -->
      <Button text="▶ JOUER"
              fx:id="btnJouer"
              onAction="#handleJouer"
              style="-fx-background-color: #27ae60;"
              textFill="WHITE"/>

   </children>
</VBox>
