<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox alignment="CENTER" maxHeight="-Infinity" maxWidth="-Infinity" minHeight="-Infinity" minWidth="-Infinity"
      prefHeight="600.0" prefWidth="900.0" spacing="0.0"
      styleClass="root-vbox"
      stylesheets="@../css/main.css"
      xmlns="http://javafx.com/javafx/8" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.triharf.controllers.ResultatsController">

    <children>
        <!-- SCROLLABLE CONTENT (Header + Details) -->
        <ScrollPane fitToWidth="true" styleClass="scroll-pane" VBox.vgrow="ALWAYS" style="-fx-background: transparent; -fx-background-color: transparent;">
            <content>
                <VBox alignment="TOP_CENTER" spacing="0.0">
                    <children>
                        <!-- En-tête -->
                        <VBox alignment="CENTER" prefHeight="150.0" prefWidth="900.0" spacing="10.0" styleClass="results-header-defeat">
                            <children>
                                <Label fx:id="lblEmoji" text="📊" style="-fx-font-size: 48px;" />
                                <Label fx:id="lblTitre" text="RÉSULTATS" style="-fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 32px;" />
                            </children>
                        </VBox>

                        <!-- Score + Encouragement -->
                        <VBox alignment="CENTER" spacing="20.0" style="-fx-padding: 30 0;">
                            <children>
                                <Label text="Score Final" styleClass="label-dim" style="-fx-font-size: 18px;" />
                                <VBox alignment="CENTER" styleClass="score-display">
                                    <children>
                                        <Label fx:id="lblScore" text="0" style="-fx-text-fill: white; -fx-font-size: 48px; -fx-font-weight: bold;" />
                                    </children>
                                </VBox>
                                <!-- Message personnalisé (required by controller) -->
                                <Label fx:id="lblMessage" text="" style="-fx-text-fill: white; -fx-font-size: 20px; -fx-font-weight: bold;" />
                                <Label fx:id="lblEncouragement" text="" wrapText="true" prefWidth="480.0" style="-fx-text-fill: #a0a0b0; -fx-font-size: 14px; -fx-font-style: italic;" />
                            </children>
                        </VBox>

                        <Separator style="-fx-opacity: 0.1; -fx-padding: 0 40;" />

                        <!-- Détails des points -->
                        <VBox alignment="TOP_CENTER" spacing="15.0" style="-fx-padding: 30 40;">
                            <children>
                                <Label text="Détails de la partie" styleClass="label-bright" style="-fx-font-size: 20px; -fx-alignment: CENTER;" />
                                <VBox fx:id="vboxDetails" spacing="10.0" />
                            </children>
                        </VBox>
                    </children>
                </VBox>
            </content>
        </ScrollPane>

        <!-- BOTTOM BUTTONS (Fixed) -->
        <HBox alignment="CENTER" spacing="20.0" style="-fx-padding: 20; -fx-background-color: rgba(0,0,0,0.2);">
            <children>
                <Button fx:id="btnReessayer" onAction="#handleReessayer" styleClass="btn-terminate" text="RÉESSAYER" />
                <Button fx:id="btnMenu" onAction="#handleMenu" styleClass="btn-secondary" text="MENU PRINCIPAL" />
            </children>
        </HBox>

    </children>
</VBox>